export enum TextureMode {
    COLOR = 1,
    ALPHA = 2,
    MASKED = 4,
    MIPMAPPED = 8,
    CLAMP_U = 16,
    CLAMP_V = 32,
    SPHERICAL_REFLECTION_MAP = 64,
    CUBIC_ENVIRONMENT_MAPPING = 128,
    VRAM = 256,
    HIGH_COLOR_TEXTURE = 512
    //TODO continue all combinations
}
